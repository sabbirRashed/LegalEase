import HeroSection from '@/components/HeroSectin';
import React from 'react';

const page = () => {
    return (
        <div>
            <HeroSection/>
        </div>
    );
};

export default page;